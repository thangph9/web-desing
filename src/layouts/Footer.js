import React from 'react';
import { Layout } from 'antd';
import GlobalFooter from '@/components/GlobalFooter';

// eslint-disable-next-line no-unused-vars
const { Footer } = Layout;
const FooterView = () => <GlobalFooter />;
export default FooterView;
